#
# Here is a dictionary containing the parameters to be fixed in `inv-gp.py`.
#
# The key must contain all of the input parameter indices, i.e. 0, 1, 2, 3, 4.
#
# The corresponding value is the fixed value of that parameter, or `None` if
# is it not fixed.
#

fix_input = {#index: value,
    0: 19000,
    1: None,  # None if not fixed
    2: 7,
    3: 1,
    4: 1,
}
